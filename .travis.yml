language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t Tahaarslan/docker-react -f Dockerfile.dev .
    
script:
    - docker run -e CI=true Tahaarslan/docker-react npm run test